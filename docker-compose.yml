

services:
  fastapi-app:
    build: .
    container_name: TextSetService
    environment:
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}
      - DB_NAME=${DB_NAME}

    ports:
      - "7000:7000"
